/*global YUI:true */
var test_config = {
    groups : {
        root : {
            /* comment one */
            modules : "configger"
        },

        subfolder : {
            /* comment two */
            modules : "configger"
        }
    }
};
